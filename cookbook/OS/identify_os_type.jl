# We can use Sys.is** functions

println("Are we running on Linux?   ", Sys.islinux())

println("Are we running on Unix?    ", Sys.isunix())

println("Are we running on Apple?   ", Sys.isapple())

println("Are we running on BSD?     ", Sys.isbsd())

println("Are we running on Windows? ", Sys.iswindows())


